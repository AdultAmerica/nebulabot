aiogram==3.30.0
APScheduler==3.11.3
SQLAlchemy==2.0.51
python-dotenv==1.2.2
aiohttp==3.14.3
# Ships the IANA timezone database so any zone an admin picks resolves, even on
# images and platforms that carry no system tzdata.
tzdata==2025.2
